# Proxy Server Configuration

# Network Settings
HOST=0.0.0.0
PORT=8888

# Concurrency
# Model: Thread-per-connection (pthread)
MAX_PENDING_CONNECTIONS=10

# Logging & Storage
LOG_FILE=proxy.log
CACHE_ENABLED=false

# Security
FILTER_FILE=config/blocked_domains.txt